{
  "name": "@pro/core",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rollup -c",
    "test": "npm run build && dotenv -e ./.env -- node ./test/index.mjs",
    "repl": "dotenv -e .env -- npm run build && node -e \"require('./build/index.cjs')\" --interactive"
  },
  "devDependencies": {
    "worker-testbed": "3.0.0"
  },
  "dependencies": {
    "zod": "3.25.76",
    "openai": "4.97.0"
  }
}
